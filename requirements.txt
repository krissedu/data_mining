# Core app
streamlit>=1.41,<2

# Vision + ML
# Pin to CPU-friendly versions known to have prebuilt wheels on Streamlit Cloud
ultralytics>=8.3.50,<9
# Match Streamlit Cloud's Python 3.13 with compatible Torch/TV versions
torch==2.8.0
torchvision==0.23.0

# Model zoo for EfficientNet/MobileNet (PyTorch)
timm==0.9.16

# Imaging
pillow==11.1.0
numpy>=2.2.0,<3
opencv-python-headless==4.10.0.84  # fixed: removed .post1 (not available for Python 3.13)

# Utils
PyYAML==6.0.1

# Note:
# - TensorFlow intentionally omitted; your code falls back to timm when TF is not present.
#   If you must use .keras weights on Cloud, set Python 3.10 and add: tensorflow==2.13.1