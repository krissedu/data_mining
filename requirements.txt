# Core app
streamlit==1.35.0

# Vision + ML
# Pin to CPU-friendly versions known to have prebuilt wheels on Streamlit Cloud
ultralytics==8.2.103
torch==2.2.2

# Model zoo for EfficientNet/MobileNet (PyTorch)
timm==0.9.16

# Imaging
pillow==10.3.0
numpy==1.26.4
opencv-python-headless==4.10.0.84

# Utils
PyYAML==6.0.1

# Note:
# - Removed torchvision (not used directly; avoids torch/torchvision ABI mismatches).
# - Removed tensorflow; your code handles its absence unless .keras models are configured.
#   If you must use .keras weights on Cloud, set Python 3.10 and add: tensorflow==2.13.1
